package com.digital.tch.common.dao;

import org.springframework.stereotype.Repository;

import com.digital.tch.common.vo.FileInfo;

@Repository
public class FileDownloadDao extends CommonDao {
	
	public FileInfo getFileInfo(int idx) throws Exception{
		
		return getSqlSession().selectOne("common.getFileInfo", idx);
	}
}
